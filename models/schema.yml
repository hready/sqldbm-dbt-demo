# ************************** SqlDBM: Snowflake *************************
# * Generated by SqlDBM: Dev - Silver Build Sprint 01 by heather.ready *


version: 2

models:
  - name: HUB_CUSTOMER
    description: 'Hub: wholesale/ERP customer business entity. Insert-only, one row per CUSTKEY ever seen.'
    config:
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - CUSTOMER_HASH_KEY
        name: pk_HUB_CUSTOMER
    columns:
      - name: CUSTOMER_HASH_KEY
        data_type: varchar(64)
        description: Hash of CUSTKEY. Sourced directly from ERP Stage STAGE_HASH_KEY.
        constraints:
          - type: not_null
      - name: CUSTKEY
        data_type: number(38,0)
        description: 'Business key: ERP wholesale customer identifier'
        constraints:
          - type: not_null
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: Source system that first introduced this business key
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this business key was first loaded
        constraints:
          - type: not_null

  - name: HUB_LOYALTY_CUSTOMER
    description: 'Hub: retail/POS loyalty customer business entity. Deliberately separate from HUB_CUSTOMER -- distinct population, not a same-as candidate. Insert-only.'
    config:
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - LOYALTY_CUSTOMER_HASH_KEY
        name: pk_HUB_LOYALTY_CUSTOMER
    columns:
      - name: LOYALTY_CUSTOMER_HASH_KEY
        data_type: varchar(64)
        description: Hash of LOYALTY_ID. Sourced directly from POS Stage STAGE_HASH_KEY.
        constraints:
          - type: not_null
      - name: LOYALTY_ID
        data_type: varchar(20)
        description: 'Business key: POS loyalty program identifier'
        constraints:
          - type: not_null
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: Source system that first introduced this business key
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this business key was first loaded
        constraints:
          - type: not_null

  - name: HUB_NATION
    description: 'Hub: nation reference entity. Insert-only.'
    config:
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - NATION_HASH_KEY
        name: pk_HUB_NATION
    columns:
      - name: NATION_HASH_KEY
        data_type: varchar(64)
        description: Hash of NATIONKEY. Sourced directly from ERP Stage STAGE_HASH_KEY.
        constraints:
          - type: not_null
      - name: NATIONKEY
        data_type: number(38,0)
        description: 'Business key: ERP nation identifier'
        constraints:
          - type: not_null
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: Source system that first introduced this business key
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this business key was first loaded
        constraints:
          - type: not_null

  - name: HUB_ORDER
    description: 'Hub: wholesale order business entity. Insert-only.'
    config:
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - ORDER_HASH_KEY
        name: pk_HUB_ORDER
    columns:
      - name: ORDER_HASH_KEY
        data_type: varchar(64)
        description: Hash of ORDERKEY. Sourced directly from ERP Stage STAGE_HASH_KEY.
        constraints:
          - type: not_null
      - name: ORDERKEY
        data_type: number(38,0)
        description: 'Business key: ERP wholesale order identifier'
        constraints:
          - type: not_null
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: Source system that first introduced this business key
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this business key was first loaded
        constraints:
          - type: not_null

  - name: HUB_PRODUCT
    description: 'Hub: product business entity. Multi-source (ERP, EDI, POS), each under its own business key. Insert-only.'
    config:
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - PRODUCT_HASH_KEY
        name: pk_HUB_PRODUCT
    columns:
      - name: PRODUCT_HASH_KEY
        data_type: varchar(64)
        description: Hash of PRODUCT_BK. Sourced directly from the contributing source's Stage STAGE_HASH_KEY.
        constraints:
          - type: not_null
      - name: PRODUCT_BK
        data_type: varchar(30)
        description: 'Business key exactly as asserted by the contributing source: ERP PARTKEY, EDI VEND_PART_CD, or POS SKU. Identity across sources is NOT resolved here -- see Silver Business same-as-link.'
        constraints:
          - type: not_null
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: Source system that asserted this specific business key
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this business key was first loaded
        constraints:
          - type: not_null

  - name: HUB_REGION
    description: 'Hub: region reference entity. Insert-only.'
    config:
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - REGION_HASH_KEY
        name: pk_HUB_REGION
    columns:
      - name: REGION_HASH_KEY
        data_type: varchar(64)
        description: Hash of REGIONKEY. Sourced directly from ERP Stage STAGE_HASH_KEY.
        constraints:
          - type: not_null
      - name: REGIONKEY
        data_type: number(38,0)
        description: 'Business key: ERP region identifier'
        constraints:
          - type: not_null
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: Source system that first introduced this business key
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this business key was first loaded
        constraints:
          - type: not_null

  - name: HUB_SALE
    description: 'Hub: retail sale business entity. Insert-only.'
    config:
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - SALE_HASH_KEY
        name: pk_HUB_SALE
    columns:
      - name: SALE_HASH_KEY
        data_type: varchar(64)
        description: Hash of SALE_TICKET_NO. Sourced directly from POS Stage STAGE_HASH_KEY.
        constraints:
          - type: not_null
      - name: SALE_TICKET_NO
        data_type: varchar(30)
        description: 'Business key: POS sale ticket identifier'
        constraints:
          - type: not_null
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: Source system that first introduced this business key
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this business key was first loaded
        constraints:
          - type: not_null

  - name: HUB_STORE
    description: 'Hub: retail store business entity. Insert-only.'
    config:
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - STORE_HASH_KEY
        name: pk_HUB_STORE
    columns:
      - name: STORE_HASH_KEY
        data_type: varchar(64)
        description: Hash of STORE_ID. Sourced directly from POS Stage STAGE_HASH_KEY.
        constraints:
          - type: not_null
      - name: STORE_ID
        data_type: varchar(20)
        description: 'Business key: POS store identifier'
        constraints:
          - type: not_null
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: Source system that first introduced this business key
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this business key was first loaded
        constraints:
          - type: not_null

  - name: HUB_SUPPLIER
    description: 'Hub: supplier business entity. Multi-source (ERP, EDI), each under its own business key. Insert-only.'
    config:
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - SUPPLIER_HASH_KEY
        name: pk_HUB_SUPPLIER
    columns:
      - name: SUPPLIER_HASH_KEY
        data_type: varchar(64)
        description: Hash of SUPPLIER_BK. Sourced directly from the contributing source's Stage STAGE_HASH_KEY.
        constraints:
          - type: not_null
      - name: SUPPLIER_BK
        data_type: varchar(20)
        description: 'Business key exactly as asserted by the contributing source: ERP SUPPLIERKEY or EDI VEND_CD. Identity across sources is NOT resolved here -- see Silver Business same-as-link.'
        constraints:
          - type: not_null
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: Source system that asserted this specific business key
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this business key was first loaded
        constraints:
          - type: not_null

  - name: LNK_CUSTOMER_NATION
    description: 'Link: a wholesale customer is based in a nation. Insert-only.'
    config:
      contract:
        enforced: true
    constraints:
      - type: foreign_key
        columns:
          - CUSTOMER_HASH_KEY
        expression: HEATHER_SILVER_RAW.HUB_CUSTOMER (CUSTOMER_HASH_KEY)
        name: fk_LNK_CUSTOMER_NATION_HUB_CUSTOMER
      - type: foreign_key
        columns:
          - NATION_HASH_KEY
        expression: HEATHER_SILVER_RAW.HUB_NATION (NATION_HASH_KEY)
        name: fk_LNK_CUSTOMER_NATION_HUB_NATION
      - type: primary_key
        columns:
          - LNK_CUSTOMER_NATION_HASH_KEY
        name: pk_LNK_CUSTOMER_NATION
    columns:
      - name: LNK_CUSTOMER_NATION_HASH_KEY
        data_type: varchar(64)
        description: Hash of the CUSTKEY + NATIONKEY combination
        constraints:
          - type: not_null
      - name: CUSTOMER_HASH_KEY
        data_type: varchar(64)
        description: FK to HUB_CUSTOMER
        constraints:
          - type: not_null
      - name: NATION_HASH_KEY
        data_type: varchar(64)
        description: FK to HUB_NATION
        constraints:
          - type: not_null
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: Source system that asserted this relationship
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this relationship was first loaded
        constraints:
          - type: not_null

  - name: LNK_NATION_REGION
    description: 'Link: a nation belongs to a region. Insert-only.'
    config:
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - LNK_NATION_REGION_HASH_KEY
        name: pk_LNK_NATION_REGION
      - type: foreign_key
        columns:
          - NATION_HASH_KEY
        expression: HEATHER_SILVER_RAW.HUB_NATION (NATION_HASH_KEY)
        name: fk_LNK_NATION_REGION_HUB_NATION
      - type: foreign_key
        columns:
          - REGION_HASH_KEY
        expression: HEATHER_SILVER_RAW.HUB_REGION (REGION_HASH_KEY)
        name: fk_LNK_NATION_REGION_HUB_REGION
    columns:
      - name: LNK_NATION_REGION_HASH_KEY
        data_type: varchar(64)
        description: Hash of the NATIONKEY + REGIONKEY combination
        constraints:
          - type: not_null
      - name: NATION_HASH_KEY
        data_type: varchar(64)
        description: FK to HUB_NATION
        constraints:
          - type: not_null
      - name: REGION_HASH_KEY
        data_type: varchar(64)
        description: FK to HUB_REGION
        constraints:
          - type: not_null
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: Source system that asserted this relationship
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this relationship was first loaded
        constraints:
          - type: not_null

  - name: LNK_ORDER
    description: 'Link: an order was placed by a customer. Insert-only.'
    config:
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - LNK_ORDER_HASH_KEY
        name: pk_LNK_ORDER
      - type: foreign_key
        columns:
          - ORDER_HASH_KEY
        expression: HEATHER_SILVER_RAW.HUB_ORDER (ORDER_HASH_KEY)
        name: fk_LNK_ORDER_HUB_ORDER
      - type: foreign_key
        columns:
          - CUSTOMER_HASH_KEY
        expression: HEATHER_SILVER_RAW.HUB_CUSTOMER (CUSTOMER_HASH_KEY)
        name: fk_LNK_ORDER_HUB_CUSTOMER
    columns:
      - name: LNK_ORDER_HASH_KEY
        data_type: varchar(64)
        description: Hash of the ORDERKEY + CUSTKEY combination
        constraints:
          - type: not_null
      - name: ORDER_HASH_KEY
        data_type: varchar(64)
        description: FK to HUB_ORDER
        constraints:
          - type: not_null
      - name: CUSTOMER_HASH_KEY
        data_type: varchar(64)
        description: FK to HUB_CUSTOMER
        constraints:
          - type: not_null
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: Source system that asserted this relationship
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this relationship was first loaded
        constraints:
          - type: not_null

  - name: LNK_ORDER_LINE
    description: 'Link: a wholesale order line ties an order to a product and a supplier. Insert-only.'
    config:
      contract:
        enforced: true
    constraints:
      - type: foreign_key
        columns:
          - PRODUCT_HASH_KEY
        expression: HEATHER_SILVER_RAW.HUB_PRODUCT (PRODUCT_HASH_KEY)
        name: fk_LNK_ORDER_LINE_HUB_PRODUCT
      - type: foreign_key
        columns:
          - SUPPLIER_HASH_KEY
        expression: HEATHER_SILVER_RAW.HUB_SUPPLIER (SUPPLIER_HASH_KEY)
        name: fk_LNK_ORDER_LINE_HUB_SUPPLIER
      - type: primary_key
        columns:
          - LNK_ORDER_LINE_HASH_KEY
        name: pk_LNK_ORDER_LINE
      - type: foreign_key
        columns:
          - ORDER_HASH_KEY
        expression: HEATHER_SILVER_RAW.HUB_ORDER (ORDER_HASH_KEY)
        name: fk_LNK_ORDER_LINE_HUB_ORDER
    columns:
      - name: LNK_ORDER_LINE_HASH_KEY
        data_type: varchar(64)
        description: Hash of the ORDERKEY + PARTKEY + SUPPKEY + LINENUMBER combination
        constraints:
          - type: not_null
      - name: ORDER_HASH_KEY
        data_type: varchar(64)
        description: FK to HUB_ORDER
        constraints:
          - type: not_null
      - name: PRODUCT_HASH_KEY
        data_type: varchar(64)
        description: FK to HUB_PRODUCT
        constraints:
          - type: not_null
      - name: SUPPLIER_HASH_KEY
        data_type: varchar(64)
        description: FK to HUB_SUPPLIER
        constraints:
          - type: not_null
      - name: LINENUMBER
        data_type: number(38,0)
        description: Degenerate key element -- distinguishes multiple lines within the same order/product/supplier combination
        constraints:
          - type: not_null
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: Source system that asserted this relationship
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this relationship was first loaded
        constraints:
          - type: not_null

  - name: LNK_PARTSUPP
    description: 'Link: a vendor EDI supply relationship ties a product to a supplier. Insert-only.'
    config:
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - LNK_PARTSUPP_HASH_KEY
        name: pk_LNK_PARTSUPP
      - type: foreign_key
        columns:
          - PRODUCT_HASH_KEY
        expression: HEATHER_SILVER_RAW.HUB_PRODUCT (PRODUCT_HASH_KEY)
        name: fk_LNK_PARTSUPP_HUB_PRODUCT
      - type: foreign_key
        columns:
          - SUPPLIER_HASH_KEY
        expression: HEATHER_SILVER_RAW.HUB_SUPPLIER (SUPPLIER_HASH_KEY)
        name: fk_LNK_PARTSUPP_HUB_SUPPLIER
    columns:
      - name: LNK_PARTSUPP_HASH_KEY
        data_type: varchar(64)
        description: Hash of the VEND_CD + VEND_PART_CD combination
        constraints:
          - type: not_null
      - name: PRODUCT_HASH_KEY
        data_type: varchar(64)
        description: FK to HUB_PRODUCT
        constraints:
          - type: not_null
      - name: SUPPLIER_HASH_KEY
        data_type: varchar(64)
        description: FK to HUB_SUPPLIER
        constraints:
          - type: not_null
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: Source system that asserted this relationship
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this relationship was first loaded
        constraints:
          - type: not_null

  - name: LNK_SALE
    description: 'Link: a retail sale ties a sale ticket to a loyalty customer (optional) and a store. Insert-only.'
    config:
      contract:
        enforced: true
    constraints:
      - type: foreign_key
        columns:
          - STORE_HASH_KEY
        expression: HEATHER_SILVER_RAW.HUB_STORE (STORE_HASH_KEY)
        name: fk_LNK_SALE_HUB_STORE
      - type: foreign_key
        columns:
          - SALE_HASH_KEY
        expression: HEATHER_SILVER_RAW.HUB_SALE (SALE_HASH_KEY)
        name: fk_LNK_SALE_HUB_SALE
      - type: foreign_key
        columns:
          - LOYALTY_CUSTOMER_HASH_KEY
        expression: HEATHER_SILVER_RAW.HUB_LOYALTY_CUSTOMER (LOYALTY_CUSTOMER_HASH_KEY)
        name: fk_LNK_SALE_HUB_LOYALTY_CUSTOMER
      - type: primary_key
        columns:
          - LNK_SALE_HASH_KEY
        name: pk_LNK_SALE
    columns:
      - name: LNK_SALE_HASH_KEY
        data_type: varchar(64)
        description: Hash of the SALE_TICKET_NO + LOYALTY_ID + STORE_ID combination
        constraints:
          - type: not_null
      - name: SALE_HASH_KEY
        data_type: varchar(64)
        description: FK to HUB_SALE
        constraints:
          - type: not_null
      - name: LOYALTY_CUSTOMER_HASH_KEY
        data_type: varchar(64)
        description: FK to HUB_LOYALTY_CUSTOMER; nullable -- not every sale is tied to a loyalty account
      - name: STORE_HASH_KEY
        data_type: varchar(64)
        description: FK to HUB_STORE
        constraints:
          - type: not_null
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: Source system that asserted this relationship
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this relationship was first loaded
        constraints:
          - type: not_null

  - name: LNK_SALE_LINE
    description: 'Link: a retail sale line ties a sale to a product. Insert-only.'
    config:
      contract:
        enforced: true
    constraints:
      - type: foreign_key
        columns:
          - PRODUCT_HASH_KEY
        expression: HEATHER_SILVER_RAW.HUB_PRODUCT (PRODUCT_HASH_KEY)
        name: fk_LNK_SALE_LINE_HUB_PRODUCT
      - type: primary_key
        columns:
          - LNK_SALE_LINE_HASH_KEY
        name: pk_LNK_SALE_LINE
      - type: foreign_key
        columns:
          - SALE_HASH_KEY
        expression: HEATHER_SILVER_RAW.HUB_SALE (SALE_HASH_KEY)
        name: fk_LNK_SALE_LINE_HUB_SALE
    columns:
      - name: LNK_SALE_LINE_HASH_KEY
        data_type: varchar(64)
        description: Hash of the SALE_TICKET_NO + SKU + LINE_SEQ combination
        constraints:
          - type: not_null
      - name: SALE_HASH_KEY
        data_type: varchar(64)
        description: FK to HUB_SALE
        constraints:
          - type: not_null
      - name: PRODUCT_HASH_KEY
        data_type: varchar(64)
        description: FK to HUB_PRODUCT
        constraints:
          - type: not_null
      - name: LINE_SEQ
        data_type: number(38,0)
        description: Degenerate key element -- distinguishes multiple lines within the same sale/product combination
        constraints:
          - type: not_null
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: Source system that asserted this relationship
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this relationship was first loaded
        constraints:
          - type: not_null

  - name: LNK_SUPPLIER_NATION
    description: 'Link: a supplier is based in a nation. Currently ERP-only (EDI does not carry nation). Insert-only.'
    config:
      contract:
        enforced: true
    constraints:
      - type: foreign_key
        columns:
          - NATION_HASH_KEY
        expression: HEATHER_SILVER_RAW.HUB_NATION (NATION_HASH_KEY)
        name: fk_LNK_SUPPLIER_NATION_HUB_NATION
      - type: foreign_key
        columns:
          - SUPPLIER_HASH_KEY
        expression: HEATHER_SILVER_RAW.HUB_SUPPLIER (SUPPLIER_HASH_KEY)
        name: fk_LNK_SUPPLIER_NATION_HUB_SUPPLIER
      - type: primary_key
        columns:
          - LNK_SUPPLIER_NATION_HASH_KEY
        name: pk_LNK_SUPPLIER_NATION
    columns:
      - name: LNK_SUPPLIER_NATION_HASH_KEY
        data_type: varchar(64)
        description: Hash of the SUPPLIER_BK + NATIONKEY combination
        constraints:
          - type: not_null
      - name: SUPPLIER_HASH_KEY
        data_type: varchar(64)
        description: FK to HUB_SUPPLIER
        constraints:
          - type: not_null
      - name: NATION_HASH_KEY
        data_type: varchar(64)
        description: FK to HUB_NATION
        constraints:
          - type: not_null
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: Source system that asserted this relationship
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this relationship was first loaded
        constraints:
          - type: not_null

  - name: SAT_CUSTOMER_ERP
    description: 'Satellite: descriptive attributes of a wholesale customer, from ERP. Insert-only, new row only when HASH_DIFF changes.'
    config:
      contract:
        enforced: true
    constraints:
      - type: foreign_key
        columns:
          - CUSTOMER_HASH_KEY
        expression: HEATHER_SILVER_RAW.HUB_CUSTOMER (CUSTOMER_HASH_KEY)
        name: fk_SAT_CUSTOMER_ERP_HUB_CUSTOMER
      - type: primary_key
        columns:
          - CUSTOMER_HASH_KEY
          - LOAD_DATE
        name: pk_SAT_CUSTOMER_ERP
    columns:
      - name: CUSTOMER_HASH_KEY
        data_type: varchar(64)
        description: FK to HUB_CUSTOMER
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this version of the row was loaded
        constraints:
          - type: not_null
      - name: HASH_DIFF
        data_type: varchar(64)
        description: Hash of all descriptive columns below -- drives insert-only change detection
        constraints:
          - type: not_null
      - name: NAME
        data_type: varchar(25)
        description: Name of the customer
      - name: ADDRESS
        data_type: varchar(40)
        description: Address of the customer
      - name: PHONE
        data_type: varchar(15)
        description: Contact phone number of the customer
      - name: ACCTBAL
        data_type: number(12,2)
        description: Account balance of the customer
      - name: MKTSEGMENT
        data_type: varchar(10)
        description: Market segment classification of the customer
      - name: COMMENT
        data_type: varchar(117)
        description: Additional comments about the customer
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: Source system for this descriptive payload
        constraints:
          - type: not_null

  - name: SAT_LOYALTY_CUSTOMER_POS
    description: 'Satellite: descriptive attributes of a retail loyalty customer, from POS. Insert-only, new row only when HASH_DIFF changes.'
    config:
      contract:
        enforced: true
    constraints:
      - type: foreign_key
        columns:
          - LOYALTY_CUSTOMER_HASH_KEY
        expression: HEATHER_SILVER_RAW.HUB_LOYALTY_CUSTOMER (LOYALTY_CUSTOMER_HASH_KEY)
        name: fk_SAT_LOYALTY_CUSTOMER_POS_HUB
      - type: primary_key
        columns:
          - LOYALTY_CUSTOMER_HASH_KEY
          - LOAD_DATE
        name: pk_SAT_LOYALTY_CUSTOMER_POS
    columns:
      - name: LOYALTY_CUSTOMER_HASH_KEY
        data_type: varchar(64)
        description: FK to HUB_LOYALTY_CUSTOMER
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this version of the row was loaded
        constraints:
          - type: not_null
      - name: HASH_DIFF
        data_type: varchar(64)
        description: Hash of all descriptive columns below -- drives insert-only change detection
        constraints:
          - type: not_null
      - name: CUST_NAME
        data_type: varchar(100)
        description: Customer name as captured at POS
      - name: CUST_PHONE
        data_type: varchar(20)
        description: Customer phone as captured at POS
      - name: SIGNUP_DATE_TXT
        data_type: varchar(20)
        description: Loyalty signup date, landed as text
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: Source system for this descriptive payload
        constraints:
          - type: not_null

  - name: SAT_NATION_ERP
    description: 'Satellite: descriptive attributes of a nation, from ERP. Region assignment lives in LNK_NATION_REGION, not here. Insert-only.'
    config:
      contract:
        enforced: true
    constraints:
      - type: foreign_key
        columns:
          - NATION_HASH_KEY
        expression: HEATHER_SILVER_RAW.HUB_NATION (NATION_HASH_KEY)
        name: fk_SAT_NATION_ERP_HUB_NATION
      - type: primary_key
        columns:
          - NATION_HASH_KEY
          - LOAD_DATE
        name: pk_SAT_NATION_ERP
    columns:
      - name: NATION_HASH_KEY
        data_type: varchar(64)
        description: FK to HUB_NATION
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this version of the row was loaded
        constraints:
          - type: not_null
      - name: HASH_DIFF
        data_type: varchar(64)
        description: Hash of all descriptive columns below -- drives insert-only change detection
        constraints:
          - type: not_null
      - name: NAME
        data_type: varchar(25)
        description: Name of the nation
      - name: COMMENT
        data_type: varchar(152)
        description: Additional comments about the nation
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: Source system for this descriptive payload
        constraints:
          - type: not_null

  - name: SAT_ORDER_ERP
    description: 'Satellite: transactional attributes of the order-customer relationship, from ERP. Insert-only, new row only when HASH_DIFF changes (e.g. order status progresses).'
    config:
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - LNK_ORDER_HASH_KEY
          - LOAD_DATE
        name: pk_SAT_ORDER_ERP
      - type: foreign_key
        columns:
          - LNK_ORDER_HASH_KEY
        expression: HEATHER_SILVER_RAW.LNK_ORDER (LNK_ORDER_HASH_KEY)
        name: fk_SAT_ORDER_ERP_LNK_ORDER
    columns:
      - name: LNK_ORDER_HASH_KEY
        data_type: varchar(64)
        description: FK to LNK_ORDER
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this version of the row was loaded
        constraints:
          - type: not_null
      - name: HASH_DIFF
        data_type: varchar(64)
        description: Hash of all descriptive columns below -- drives insert-only change detection
        constraints:
          - type: not_null
      - name: ORDERSTATUS
        data_type: varchar(1)
        description: Status of the order (O/F/P)
      - name: TOTALPRICE
        data_type: number(12,2)
        description: Total price of the order
      - name: ORDERDATE
        data_type: date
        description: Date when the order was placed
      - name: ORDERPRIORITY
        data_type: varchar(15)
        description: Priority of the order
      - name: CLERK
        data_type: varchar(15)
        description: Clerk responsible for the order
      - name: SHIPPRIORITY
        data_type: number(38,0)
        description: Shipping priority of the order
      - name: COMMENT
        data_type: varchar(79)
        description: Additional comments about the order
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: Source system for this descriptive payload
        constraints:
          - type: not_null

  - name: SAT_ORDER_LINE_ERP
    description: 'Satellite: transactional attributes of a wholesale order line, from ERP. Insert-only, new row only when HASH_DIFF changes.'
    config:
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - LNK_ORDER_LINE_HASH_KEY
          - LOAD_DATE
        name: pk_SAT_ORDER_LINE_ERP
      - type: foreign_key
        columns:
          - LNK_ORDER_LINE_HASH_KEY
        expression: HEATHER_SILVER_RAW.LNK_ORDER_LINE (LNK_ORDER_LINE_HASH_KEY)
        name: fk_SAT_ORDER_LINE_ERP_LNK_ORDER_LINE
    columns:
      - name: LNK_ORDER_LINE_HASH_KEY
        data_type: varchar(64)
        description: FK to LNK_ORDER_LINE
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this version of the row was loaded
        constraints:
          - type: not_null
      - name: HASH_DIFF
        data_type: varchar(64)
        description: Hash of all descriptive columns below -- drives insert-only change detection
        constraints:
          - type: not_null
      - name: QUANTITY
        data_type: number(12,2)
        description: Quantity of the item ordered
      - name: EXTENDEDPRICE
        data_type: number(12,2)
        description: Extended price of the item
      - name: DISCOUNT
        data_type: number(12,4)
        description: Discount rate applied to the item, as a decimal fraction
      - name: TAX
        data_type: number(12,4)
        description: Tax rate applied to the item, as a decimal fraction
      - name: RETURNFLAG
        data_type: varchar(1)
        description: Return status of the item
      - name: LINESTATUS
        data_type: varchar(1)
        description: Status of the line item
      - name: SHIPDATE
        data_type: date
        description: Date the item was shipped
      - name: COMMITDATE
        data_type: date
        description: Date the item was committed for shipping
      - name: RECEIPTDATE
        data_type: date
        description: Date the item was received
      - name: SHIPINSTRUCT
        data_type: varchar(25)
        description: Shipping instructions for the item
      - name: SHIPMODE
        data_type: varchar(10)
        description: Shipping mode for the item
      - name: COMMENT
        data_type: varchar(44)
        description: Additional comments about the line item
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: Source system for this descriptive payload
        constraints:
          - type: not_null

  - name: SAT_PARTSUPP_EDI
    description: 'Satellite: descriptive attributes of the product-supplier supply relationship, from the vendor EDI feed. Insert-only, new row only when HASH_DIFF changes (e.g. lead time or cost changes).'
    config:
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - LNK_PARTSUPP_HASH_KEY
          - LOAD_DATE
        name: pk_SAT_PARTSUPP_EDI
      - type: foreign_key
        columns:
          - LNK_PARTSUPP_HASH_KEY
        expression: HEATHER_SILVER_RAW.LNK_PARTSUPP (LNK_PARTSUPP_HASH_KEY)
        name: fk_SAT_PARTSUPP_EDI_LNK_PARTSUPP
    columns:
      - name: LNK_PARTSUPP_HASH_KEY
        data_type: varchar(64)
        description: FK to LNK_PARTSUPP
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this version of the row was loaded
        constraints:
          - type: not_null
      - name: HASH_DIFF
        data_type: varchar(64)
        description: Hash of all descriptive columns below -- drives insert-only change detection
        constraints:
          - type: not_null
      - name: AVAIL_QTY_TXT
        data_type: varchar(20)
        description: Available quantity, landed as text per raw EDI convention -- cast/cleansed in Silver Business
      - name: LEAD_TM_DAYS
        data_type: varchar(10)
        description: Lead time in days, landed as text
      - name: SUPPLY_CST_AMT
        data_type: varchar(20)
        description: Supply cost, landed as text per raw EDI convention -- cast/cleansed in Silver Business
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: Source system for this descriptive payload
        constraints:
          - type: not_null

  - name: SAT_PRODUCT_EDI
    description: 'Satellite: descriptive attributes of a product, from the vendor EDI feed. Insert-only, new row only when HASH_DIFF changes. Note: POS does not contribute a product satellite -- it only asserts the SKU business key via LNK_SALE_LINE.'
    config:
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - PRODUCT_HASH_KEY
          - LOAD_DATE
        name: pk_SAT_PRODUCT_EDI
      - type: foreign_key
        columns:
          - PRODUCT_HASH_KEY
        expression: HEATHER_SILVER_RAW.HUB_PRODUCT (PRODUCT_HASH_KEY)
        name: fk_SAT_PRODUCT_EDI_HUB_PRODUCT
    columns:
      - name: PRODUCT_HASH_KEY
        data_type: varchar(64)
        description: FK to HUB_PRODUCT
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this version of the row was loaded
        constraints:
          - type: not_null
      - name: HASH_DIFF
        data_type: varchar(64)
        description: Hash of all descriptive columns below -- drives insert-only change detection
        constraints:
          - type: not_null
      - name: VEND_PART_DESC
        data_type: varchar(200)
        description: Vendor's part description text
      - name: UNIT_CST_AMT
        data_type: varchar(20)
        description: Unit cost, landed as text per raw EDI convention -- cast/cleansed in Silver Business
      - name: PKG_CD
        data_type: varchar(10)
        description: Vendor packaging code
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: Source system for this descriptive payload
        constraints:
          - type: not_null

  - name: SAT_PRODUCT_ERP
    description: 'Satellite: descriptive attributes of a product, from ERP. Insert-only, new row only when HASH_DIFF changes.'
    config:
      contract:
        enforced: true
    constraints:
      - type: foreign_key
        columns:
          - PRODUCT_HASH_KEY
        expression: HEATHER_SILVER_RAW.HUB_PRODUCT (PRODUCT_HASH_KEY)
        name: fk_SAT_PRODUCT_ERP_HUB_PRODUCT
      - type: primary_key
        columns:
          - PRODUCT_HASH_KEY
          - LOAD_DATE
        name: pk_SAT_PRODUCT_ERP
    columns:
      - name: PRODUCT_HASH_KEY
        data_type: varchar(64)
        description: FK to HUB_PRODUCT
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this version of the row was loaded
        constraints:
          - type: not_null
      - name: HASH_DIFF
        data_type: varchar(64)
        description: Hash of all descriptive columns below -- drives insert-only change detection
        constraints:
          - type: not_null
      - name: NAME
        data_type: varchar(55)
        description: Name of the part
      - name: MFGR
        data_type: varchar(25)
        description: Manufacturer of the part
      - name: BRAND
        data_type: varchar(10)
        description: Brand of the part
      - name: TYPE
        data_type: varchar(25)
        description: Type or category of the part
      - name: SIZE
        data_type: number(38,0)
        description: Size of the part
      - name: CONTAINER
        data_type: varchar(10)
        description: Container type used for the part
      - name: RETAILPRICE
        data_type: number(12,2)
        description: Retail price of the part
      - name: COMMENT
        data_type: varchar(23)
        description: Additional comments about the part
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: Source system for this descriptive payload
        constraints:
          - type: not_null

  - name: SAT_REGION_ERP
    description: 'Satellite: descriptive attributes of a region, from ERP. Insert-only.'
    config:
      contract:
        enforced: true
    constraints:
      - type: foreign_key
        columns:
          - REGION_HASH_KEY
        expression: HEATHER_SILVER_RAW.HUB_REGION (REGION_HASH_KEY)
        name: fk_SAT_REGION_ERP_HUB_REGION
      - type: primary_key
        columns:
          - REGION_HASH_KEY
          - LOAD_DATE
        name: pk_SAT_REGION_ERP
    columns:
      - name: REGION_HASH_KEY
        data_type: varchar(64)
        description: FK to HUB_REGION
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this version of the row was loaded
        constraints:
          - type: not_null
      - name: HASH_DIFF
        data_type: varchar(64)
        description: Hash of all descriptive columns below -- drives insert-only change detection
        constraints:
          - type: not_null
      - name: NAME
        data_type: varchar(25)
        description: Name of the region
      - name: COMMENT
        data_type: varchar(152)
        description: Additional comments about the region
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: Source system for this descriptive payload
        constraints:
          - type: not_null

  - name: SAT_SALE_LINE_POS
    description: 'Satellite: transactional attributes of a retail sale line, from POS. Insert-only, new row only when HASH_DIFF changes.'
    config:
      contract:
        enforced: true
    constraints:
      - type: foreign_key
        columns:
          - LNK_SALE_LINE_HASH_KEY
        expression: HEATHER_SILVER_RAW.LNK_SALE_LINE (LNK_SALE_LINE_HASH_KEY)
        name: fk_SAT_SALE_LINE_POS_LNK_SALE_LINE
      - type: primary_key
        columns:
          - LNK_SALE_LINE_HASH_KEY
          - LOAD_DATE
        name: pk_SAT_SALE_LINE_POS
    columns:
      - name: LNK_SALE_LINE_HASH_KEY
        data_type: varchar(64)
        description: FK to LNK_SALE_LINE
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this version of the row was loaded
        constraints:
          - type: not_null
      - name: HASH_DIFF
        data_type: varchar(64)
        description: Hash of all descriptive columns below -- drives insert-only change detection
        constraints:
          - type: not_null
      - name: QTY_SOLD
        data_type: number(12,2)
        description: Quantity sold on this line
      - name: UNIT_PRICE
        data_type: number(12,2)
        description: Unit price at time of sale
      - name: DISCOUNT_PCT
        data_type: number(5,2)
        description: Discount percentage applied to the line
      - name: LINE_TOTAL
        data_type: number(12,2)
        description: Extended total for the line
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: Source system for this descriptive payload
        constraints:
          - type: not_null

  - name: SAT_SALE_POS
    description: 'Satellite: transactional attributes of the sale relationship, from POS. Insert-only, new row only when HASH_DIFF changes (e.g. a sale is later marked VOIDED).'
    config:
      contract:
        enforced: true
    constraints:
      - type: foreign_key
        columns:
          - LNK_SALE_HASH_KEY
        expression: HEATHER_SILVER_RAW.LNK_SALE (LNK_SALE_HASH_KEY)
        name: fk_SAT_SALE_POS_LNK_SALE
      - type: primary_key
        columns:
          - LNK_SALE_HASH_KEY
          - LOAD_DATE
        name: pk_SAT_SALE_POS
    columns:
      - name: LNK_SALE_HASH_KEY
        data_type: varchar(64)
        description: FK to LNK_SALE
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this version of the row was loaded
        constraints:
          - type: not_null
      - name: HASH_DIFF
        data_type: varchar(64)
        description: Hash of all descriptive columns below -- drives insert-only change detection
        constraints:
          - type: not_null
      - name: SALE_DATE_TXT
        data_type: varchar(20)
        description: Sale date, landed as text
      - name: SALE_STATUS
        data_type: varchar(20)
        description: POS sale status (e.g. COMPLETED, VOIDED, RETURNED)
      - name: CASHIER_NAME
        data_type: varchar(100)
        description: Name of the cashier who processed the sale
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: Source system for this descriptive payload
        constraints:
          - type: not_null

  - name: SAT_STORE_POS
    description: 'Satellite: descriptive attributes of a retail store, from POS. Insert-only, new row only when HASH_DIFF changes.'
    config:
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - STORE_HASH_KEY
          - LOAD_DATE
        name: pk_SAT_STORE_POS
      - type: foreign_key
        columns:
          - STORE_HASH_KEY
        expression: HEATHER_SILVER_RAW.HUB_STORE (STORE_HASH_KEY)
        name: fk_SAT_STORE_POS_HUB_STORE
    columns:
      - name: STORE_HASH_KEY
        data_type: varchar(64)
        description: FK to HUB_STORE
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this version of the row was loaded
        constraints:
          - type: not_null
      - name: HASH_DIFF
        data_type: varchar(64)
        description: Hash of all descriptive columns below -- drives insert-only change detection
        constraints:
          - type: not_null
      - name: STORE_NAME
        data_type: varchar(100)
        description: Store display name
      - name: STORE_REGION_TXT
        data_type: varchar(50)
        description: Free-text region name as captured at POS, not conformed to HUB_REGION
      - name: OPEN_DATE_TXT
        data_type: varchar(20)
        description: Store open date, landed as text
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: Source system for this descriptive payload
        constraints:
          - type: not_null

  - name: SAT_SUPPLIER_EDI
    description: 'Satellite: descriptive attributes of a supplier, from the vendor EDI feed. Insert-only, new row only when HASH_DIFF changes.'
    config:
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - SUPPLIER_HASH_KEY
          - LOAD_DATE
        name: pk_SAT_SUPPLIER_EDI
      - type: foreign_key
        columns:
          - SUPPLIER_HASH_KEY
        expression: HEATHER_SILVER_RAW.HUB_SUPPLIER (SUPPLIER_HASH_KEY)
        name: fk_SAT_SUPPLIER_EDI_HUB_SUPPLIER
    columns:
      - name: SUPPLIER_HASH_KEY
        data_type: varchar(64)
        description: FK to HUB_SUPPLIER
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this version of the row was loaded
        constraints:
          - type: not_null
      - name: HASH_DIFF
        data_type: varchar(64)
        description: Hash of all descriptive columns below -- drives insert-only change detection
        constraints:
          - type: not_null
      - name: VEND_NM
        data_type: varchar(100)
        description: Vendor name
      - name: VEND_ADDR
        data_type: varchar(150)
        description: Vendor address, single unparsed field
      - name: VEND_PHN
        data_type: varchar(20)
        description: Vendor phone number
      - name: VEND_BAL_AMT
        data_type: varchar(20)
        description: Vendor account balance, landed as text per raw EDI convention -- cast/cleansed in Silver Business
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: Source system for this descriptive payload
        constraints:
          - type: not_null

  - name: SAT_SUPPLIER_ERP
    description: 'Satellite: descriptive attributes of a supplier, from ERP. Insert-only, new row only when HASH_DIFF changes.'
    config:
      contract:
        enforced: true
    constraints:
      - type: foreign_key
        columns:
          - SUPPLIER_HASH_KEY
        expression: HEATHER_SILVER_RAW.HUB_SUPPLIER (SUPPLIER_HASH_KEY)
        name: fk_SAT_SUPPLIER_ERP_HUB_SUPPLIER
      - type: primary_key
        columns:
          - SUPPLIER_HASH_KEY
          - LOAD_DATE
        name: pk_SAT_SUPPLIER_ERP
    columns:
      - name: SUPPLIER_HASH_KEY
        data_type: varchar(64)
        description: FK to HUB_SUPPLIER
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this version of the row was loaded
        constraints:
          - type: not_null
      - name: HASH_DIFF
        data_type: varchar(64)
        description: Hash of all descriptive columns below -- drives insert-only change detection
        constraints:
          - type: not_null
      - name: SUPPLIERNAME
        data_type: varchar(25)
        description: Name of the supplier
      - name: ADDRESS
        data_type: varchar(40)
        description: Address of the supplier
      - name: PHONE
        data_type: varchar(15)
        description: Contact phone number of the supplier
      - name: ACCOUNTBALANCE
        data_type: number(12,2)
        description: Account balance of the supplier
      - name: COMMENT
        data_type: varchar(303)
        description: Additional comments about the supplier
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: Source system for this descriptive payload
        constraints:
          - type: not_null


# ************************** SqlDBM: Snowflake *************************
# * Generated by SqlDBM: Dev - Silver Build Sprint 01 by heather.ready *


version: 2

models:
  - name: SAL_PRODUCT
    description: 'Same-as-link: asserts that two HUB_PRODUCT rows (typically from different sources) represent the same real-world product. Insert-only.'
    config:
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - SAL_PRODUCT_HASH_KEY
        name: pk_SAL_PRODUCT
      - type: foreign_key
        columns:
          - PRODUCT_HASH_KEY_1
        expression: HEATHER_SILVER_RAW.HUB_PRODUCT (PRODUCT_HASH_KEY)
        name: fk_SAL_PRODUCT_HUB_PRODUCT_1
      - type: foreign_key
        columns:
          - PRODUCT_HASH_KEY_2
        expression: HEATHER_SILVER_RAW.HUB_PRODUCT (PRODUCT_HASH_KEY)
        name: fk_SAL_PRODUCT_HUB_PRODUCT_2
    columns:
      - name: SAL_PRODUCT_HASH_KEY
        data_type: varchar(64)
        description: Hash of the two participating PRODUCT_HASH_KEY values (normalized ordering to avoid duplicate reversed pairs)
        constraints:
          - type: not_null
      - name: PRODUCT_HASH_KEY_1
        data_type: varchar(64)
        description: FK to HEATHER_SILVER_RAW.HUB_PRODUCT -- one side of the match
        constraints:
          - type: not_null
      - name: PRODUCT_HASH_KEY_2
        data_type: varchar(64)
        description: FK to HEATHER_SILVER_RAW.HUB_PRODUCT -- the other side of the match
        constraints:
          - type: not_null
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: 'Not a source system -- this is a derived judgment. Convention: DBT_DERIVED'
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this match was first asserted
        constraints:
          - type: not_null

  - name: SAL_SUPPLIER
    description: 'Same-as-link: asserts that two HUB_SUPPLIER rows (ERP and EDI) represent the same real-world supplier. Insert-only.'
    config:
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - SAL_SUPPLIER_HASH_KEY
        name: pk_SAL_SUPPLIER
      - type: foreign_key
        columns:
          - SUPPLIER_HASH_KEY_1
        expression: HEATHER_SILVER_RAW.HUB_SUPPLIER (SUPPLIER_HASH_KEY)
        name: fk_SAL_SUPPLIER_HUB_SUPPLIER_1
      - type: foreign_key
        columns:
          - SUPPLIER_HASH_KEY_2
        expression: HEATHER_SILVER_RAW.HUB_SUPPLIER (SUPPLIER_HASH_KEY)
        name: fk_SAL_SUPPLIER_HUB_SUPPLIER_2
    columns:
      - name: SAL_SUPPLIER_HASH_KEY
        data_type: varchar(64)
        description: Hash of the two participating SUPPLIER_HASH_KEY values (normalized ordering to avoid duplicate reversed pairs)
        constraints:
          - type: not_null
      - name: SUPPLIER_HASH_KEY_1
        data_type: varchar(64)
        description: FK to HEATHER_SILVER_RAW.HUB_SUPPLIER -- one side of the match
        constraints:
          - type: not_null
      - name: SUPPLIER_HASH_KEY_2
        data_type: varchar(64)
        description: FK to HEATHER_SILVER_RAW.HUB_SUPPLIER -- the other side of the match
        constraints:
          - type: not_null
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: 'Not a source system -- this is a derived judgment. Convention: DBT_DERIVED'
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this match was first asserted
        constraints:
          - type: not_null

  - name: SAT_CUSTOMER_BUSINESS
    description: "Satellite: derived customer tier, computed via business rule -- not asserted by any source. Insert-only, new row only when HASH_DIFF changes (i.e. the customer's tier actually changes)."
    config:
      contract:
        enforced: true
    constraints:
      - type: foreign_key
        columns:
          - CUSTOMER_HASH_KEY
        expression: HEATHER_SILVER_RAW.HUB_CUSTOMER (CUSTOMER_HASH_KEY)
        name: fk_SAT_CUSTOMER_BUSINESS_HUB_CUSTOMER
      - type: primary_key
        columns:
          - CUSTOMER_HASH_KEY
          - LOAD_DATE
        name: pk_SAT_CUSTOMER_BUSINESS
    columns:
      - name: CUSTOMER_HASH_KEY
        data_type: varchar(64)
        description: FK to HEATHER_SILVER_RAW.HUB_CUSTOMER
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this version of the row was computed
        constraints:
          - type: not_null
      - name: HASH_DIFF
        data_type: varchar(64)
        description: Hash of all descriptive columns below -- drives insert-only change detection
        constraints:
          - type: not_null
      - name: CUSTOMER_TIER
        data_type: varchar(20)
        description: Derived business classification (e.g. HIGH_VALUE, STANDARD, AT_RISK), computed from ACCTBAL and MKTSEGMENT in SAT_CUSTOMER_ERP. Thresholds are a demo placeholder -- adjust to fit whatever business rule you want to show.
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: 'Not a source system -- this is calculated. Convention: DBT_DERIVED'
        constraints:
          - type: not_null

  - name: SAT_SAL_PRODUCT_MATCH
    description: 'Satellite: rationale for a product same-as match -- not asserted by any source, computed by dbt matching logic. Insert-only.'
    config:
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - SAL_PRODUCT_HASH_KEY
          - LOAD_DATE
        name: pk_SAT_SAL_PRODUCT_MATCH
      - type: foreign_key
        columns:
          - SAL_PRODUCT_HASH_KEY
        expression: HEATHER_SILVER_BUSINESS.SAL_PRODUCT (SAL_PRODUCT_HASH_KEY)
        name: fk_SAT_SAL_PRODUCT_MATCH_SAL_PRODUCT
    columns:
      - name: SAL_PRODUCT_HASH_KEY
        data_type: varchar(64)
        description: FK to SAL_PRODUCT
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this version of the match rationale was recorded
        constraints:
          - type: not_null
      - name: HASH_DIFF
        data_type: varchar(64)
        description: Hash of all descriptive columns below -- drives insert-only change detection
        constraints:
          - type: not_null
      - name: MATCH_METHOD
        data_type: varchar(50)
        description: How the match was determined, e.g. EXACT_DESCRIPTION_MATCH, MANUAL_CROSSREF, FUZZY_NAME_MATCH. Demo placeholder -- adjust to whatever matching logic you want to show.
      - name: MATCH_CONFIDENCE
        data_type: number(5,2)
        description: Confidence score for the match, 0-100. Demo placeholder.
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: 'Not a source system -- this is derived. Convention: DBT_DERIVED'
        constraints:
          - type: not_null

  - name: SAT_SAL_SUPPLIER_MATCH
    description: 'Satellite: rationale for a supplier same-as match -- not asserted by any source, computed by dbt matching logic. Insert-only.'
    config:
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - SAL_SUPPLIER_HASH_KEY
          - LOAD_DATE
        name: pk_SAT_SAL_SUPPLIER_MATCH
      - type: foreign_key
        columns:
          - SAL_SUPPLIER_HASH_KEY
        expression: HEATHER_SILVER_BUSINESS.SAL_SUPPLIER (SAL_SUPPLIER_HASH_KEY)
        name: fk_SAT_SAL_SUPPLIER_MATCH_SAL_SUPPLIER
    columns:
      - name: SAL_SUPPLIER_HASH_KEY
        data_type: varchar(64)
        description: FK to SAL_SUPPLIER
        constraints:
          - type: not_null
      - name: LOAD_DATE
        data_type: timestamp_ltz(9)
        description: Timestamp this version of the match rationale was recorded
        constraints:
          - type: not_null
      - name: HASH_DIFF
        data_type: varchar(64)
        description: Hash of all descriptive columns below -- drives insert-only change detection
        constraints:
          - type: not_null
      - name: MATCH_METHOD
        data_type: varchar(50)
        description: How the match was determined, e.g. EXACT_NAME_MATCH, MANUAL_CROSSREF, TAX_ID_MATCH. Demo placeholder.
      - name: MATCH_CONFIDENCE
        data_type: number(5,2)
        description: Confidence score for the match, 0-100. Demo placeholder.
      - name: RECORD_SOURCE
        data_type: varchar(50)
        description: 'Not a source system -- this is derived. Convention: DBT_DERIVED'
        constraints:
          - type: not_null

  - name: STG_POS_SALE_LINE
    description: Staging table for POS sale line items. Iceberg table type -- fit-for-purpose for high-volume raw event data with external, cost-efficient storage. Append-only.
    access: protected
    config:
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - STAGE_HASH_KEY
          - LOAD_TIMESTAMP
        name: pk_STG_POS_SALE_LINE
    columns:
      - name: STAGE_HASH_KEY
        data_type: varchar(64)
        description: "Hash of this table's own composite business key: SALE_TICKET_NO + LINE_SEQ"
        constraints:
          - type: not_null
      - name: SALE_TICKET_NO
        data_type: varchar(30)
        description: Referenced sale business key -- virtual relationship only
        constraints:
          - type: not_null
      - name: LINE_SEQ
        data_type: number(38,0)
        description: Line sequence number within the ticket (part of this table's own business key)
        constraints:
          - type: not_null
      - name: SKU
        data_type: varchar(30)
        description: POS stock-keeping unit -- different part identifier flavor than ERP PARTKEY
      - name: QTY_SOLD
        data_type: number(12,2)
        description: Quantity sold on this line
      - name: UNIT_PRICE
        data_type: number(12,2)
        description: Unit price at time of sale
      - name: DISCOUNT_PCT
        data_type: number(5,2)
        description: Discount percentage applied to the line
      - name: LINE_TOTAL
        data_type: number(12,2)
        description: Extended total for the line
      - name: SALE_DATE_TXT
        data_type: varchar(20)
        description: Sale date landed as text, denormalized from header for scan efficiency
      - name: FILE_LOAD_DATE
        data_type: varchar(20)
        description: Source-asserted file date, landed as text
      - name: SOURCE_SYSTEM
        data_type: varchar(50)
        description: Source system code
        constraints:
          - type: not_null
      - name: LOAD_TIMESTAMP
        data_type: timestamp_ltz(9)
        description: System-generated timestamp of this load run
        constraints:
          - type: not_null

